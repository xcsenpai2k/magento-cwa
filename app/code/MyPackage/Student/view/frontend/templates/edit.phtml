<?php
/** @var $block \MyPackage\Student\Block\Page */
$student = $block->getStudent();
?>

<form action="<?php echo $block->getFormAction(); ?>" method="post" id="edit-student-form">
    <input name="form_key" type="hidden" value="<?php echo $block->getFormKey(); ?>">

    <div class="field">
        <label for="entity_id">Entity ID</label>
        <input type="text" name="entity_id" id="entity_id" value="<?php echo $student->getEntityId(); ?>" readonly>
    </div>

    <div class="field">
        <label for="name">Name</label>
        <input type="text" name="name" id="name" value="<?php echo $student->getName(); ?>">
    </div>

    <div class="field">
        <label for="gender">Gender</label>
        <select name="gender" id="gender">
            <option value="0" <?php if (!$student->getGender()) echo 'selected'; ?>>Male</option>
            <option value="1" <?php if ($student->getGender()) echo 'selected'; ?>>Female</option>
        </select>
    </div>

    <div class="field">
        <label for="dob">Date of Birth</label>
        <input type="date" name="dob" id="dob" value="<?php echo $student->getDob(); ?>">
    </div>

    <div class="field">
        <label for="address">Address</label>
        <input type="text" name="address" id="address" value="<?php echo $student->getAddress(); ?>">
    </div>

    <div class="actions">
        <button type="submit">Save</button>
    </div>
</form>
